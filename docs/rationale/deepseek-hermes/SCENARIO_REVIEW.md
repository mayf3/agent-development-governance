# 实操指南的作者正反案例

状态：非权威、作者桌面检查；不是独立语义审查或产品运行测试。

```text
BASE_HEAD = 99ad23923f341d7490af2b785322032507eca910
RECORDED_AT = 2026-09-18
METHOD = text-based author scenario walkthrough
INDEPENDENT_REVIEW = NOT_PERFORMED
PRODUCT_TESTS = NOT_RUN
PRODUCTION_OPERATION = NOT_PERFORMED
```

依据 [实操指南](../../guides/EVIDENCE_LED_REFACTORING.md) 与 [来源取舍](SOURCE_REVIEW.md) 检查可选方法是否误导授权、证据或范围。下表的判断是设计反例，不意味着运行过描述中的命令。

| ID | 输入情景 | 期望处置 | 对应指南 |
|---|---|---|---|
| S01 | grep 找不到导出调用，但插件可动态导入 | 保持外部/动态消费者未知，调查后再定，不按死代码删除 | 2.1 |
| S02 | 只有测试使用某 API，测试保护 accepted 兼容承诺 | 保留相应承诺；不能为了简化删除失败测试 | 2.1、3 |
| S03 | 新旧状态镜像同一事实，无独立业务价值 | 核对真正拥有者与使用者，提出收敛候选并说明退出证据 | 2.1、2.4 |
| S04 | 手写 parser 可被依赖替代，但需要同样庞大的 wrapper | 报告残余语义和依赖成本，不凭净删行数宣布胜利 | 2.4 |
| S05 | 多个调用点同一缺陷，局部 fix 只为 caller A 加例外 | 提出 altitude 候选；共享重构超出授权则单独报告而非擅自扩大 | 2.2 |
| S06 | 有意隔离供应商版本的 adapter 看起来像多余转发 | 查原始理由和合同；确有边界时保留 | 2.2、2.3 |
| S07 | 批量合并分支改变匹配优先级，单测 expected 一起刷新 | 以原语义与负例检验转换，不把更新 expected 当行为保持 | 3 |
| S08 | 移动模块，源码 import 可用，但 wheel 未包含新文件 | 用适用制品安装/导出 smoke 查缺口，源码测试不足以通过发布判断 | 3、5 |
| S09 | 两个 worker 分支各自全绿，共同修改的 helper 整合后失配 | 对整合树验证；不能把分支 PASS 相加 | 4 |
| S10 | parent 合并 worker tip 后，worker 又提交两个修复 | 最终 HEAD 停写确认与提交对账；未纳入的尾部重新收口 | 4 |
| S11 | squash 导入全部变更，但 ancestry 显示源分支未合并 | 用提交映射和内容核对，不误用零未合并提交作唯一证明 | 4 |
| S12 | worker 显示 completed，摘要截断且产物缺失 | 报告交付未确认，核对产物与任务存活；不直接重派未知副作用任务 | 4 |
| S13 | 不同工作树共用固定端口和目录，整合测试偶发冲突 | 隔离共享资源并证明 readiness/清理，不假设 worktree 已隔离宿主 | 4、5 |
| S14 | actual/expected 都调用同一 helper | 可证明调用关系，不能独立证明该 helper 的语义正确 | 5 |
| S15 | 更长 timeout 和重跑使测试变绿，但没有定位等待条件 | 保持根因未知；有据的外部 retry 或恢复原 lane 预算另作说明 | 5 |
| S16 | 删掉重复说明时也删掉取消后的副作用例外和 WHY | 恢复必要命题；可见 prompt/错误文案按行为判断 | 6 |
| S17 | 老的 rejected Note 很长，但仍能阻止一个常见错误设计 | 按未来价值保留，并服从本地记录生命周期；不按年龄/字数删除 | 6 |
| S18 | 四 Agent 都同意删除一项，另一位找到真实外部使用 | 采纳可验证反证而非票数；保留或走合法迁移 | 2.3 |
| S19 | 减少 LOC，但导入成本上升；lookup 模拟 token 下降 | 同报收益与代价，区分模拟与真实任务效果，不作总节省承诺 | 7 |
| S20 | 内部小修复被要求启动 1,393 个 Agent、建新清单平台 | 拒绝规模崇拜；单人/小批完成适用方法，不扩大任务 | 1、4、7 |
| S21 | 用户仅要求调查，Skill 将 SAFE 标签当成删除许可 | 保持只读；风险标签不构成执行授权 | 1 |
| S22 | 合并本指南后，宣称所有消费者已启用新治理 | 只报告参考内容合入；分发、采用和运行验证没有自动发生 | 9 |

作者检查的结果：上述场景均能在指南中找到明确处置路径。它检验的是文档的适用范围与表达，不是工具强制效果；没有声明新的规则验证器、测试执行或独立 PASS。
