# Agent Development Governance v1.0.2

```text
RELEASE_VERSION = 1.0.2
RELEASE_TAG = v1.0.2
RELEASE_DATE = 2026-09-03
RELEASE_BASE = 4a08770792ce96c1183dbf97c908950c6ba492a3
RELEASE_STATUS = CANDIDATE_UNTIL_EXACT_HEAD_REVIEW_AND_TAG_PUBLICATION
```

## Purpose

v1.0.2 is a forward-only publication remediation. It carries forward the
v1.0.1 transition-validator behavior unchanged while repairing the release
artifact and exact-Head review record that were incomplete when v1.0.1 was
published.

The supported lifecycle remains:

```text
preparation:
  predecessor.status = accepted
  predecessor.superseded_by = null
  successor.status = proposed
  successor.supersedes = [predecessor]

final acceptance:
  predecessor.status = superseded
  predecessor.superseded_by = successor
  successor.status = accepted
  successor.supersedes = [predecessor]
```

## Named consumer boundary

At candidate preparation time, `mayf3/svc-workflow` PR #22 is still Open,
Draft, and unmerged at Head
`7747b9387c922069b4968d776ef102a95f33bc36`. It has already been re-vendored to
v1.0.1, but this upstream release does not modify, accept, mark Ready, or merge
that PR.

After v1.0.2 is published, PR #22 must re-vendor the exact annotated v1.0.2 tag
commit, rerun its repository-local governance and Rust checks in a clean
worktree, receive a new independent adoption audit bound to its new exact Head,
and complete Owner acceptance before merge.

## Historical release handling

`v1.0.0` and `v1.0.1` remain immutable. Neither tag may be moved, overwritten,
or silently reused. v1.0.1 remains historical evidence of the technical fix,
but new consumer adoption should use v1.0.2 because v1.0.1 was published before
its required named-consumer release-note gate and exact-final-Head review record
were closed.

## Exact scope

This candidate changes only distribution version metadata, root release
documentation, the deterministic manifest, the historical v1.0.1 notice, and
this v1.0.2 release note. It does not change the transition validator, its 73
existing tests, accepted Product Authority Specs, Operational Layer, consumers,
product code, runtime, permissions, Credentials, GitHub settings, or production.

## Publication gate

Before publication, an independent full patch-release review must bind the
exact v1.0.2 Head and verify:

- the validator and transition-test blobs are unchanged from v1.0.1;
- 73/73 tests, manifest check, Python compile, and shell parse pass on the exact
  candidate tree;
- the accepted Governance V1 and Operational Layer Spec blobs are unchanged;
- this file explicitly records the live `svc-workflow` PR #22 boundary and the
  required re-vendor/local-review path;
- v1.0.0 and v1.0.1 tags remain unchanged;
- v1.0.2 tag, GitHub Release, merge, and consumer adoption have not occurred.
